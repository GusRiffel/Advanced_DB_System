<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8"/>
    <meta
            name="viewport"
            content="width=device-width, initial-scale=1, shrink-to-fit=no"
    />
    <meta name="description" content=""/>
    <title>ParkHeroes</title>

    <link
            rel="canonical"
            href="https://getbootstrap.com/docs/4.5/examples/album/"
    />

    <!-- Bootstrap core CSS -->
    <link
            rel="stylesheet"
            href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css"
            integrity="sha384-JcKb8q3iqJ61gNV9KGb8thSsNjpSL0n8PARn9HuZOnIxN0hoP+VmmDGMN5t9UJ0Z"
            crossorigin="anonymous"
    />

    <link href="css/album.css" rel="stylesheet"/>
</head>
<body>
<%- include('common/header'); -%>
<div class="container">

    <% if (message) { %>
        <div class="row justify-content-md-center">
            <div class="alert alert-primary" role="alert">
                <%= message %>
            </div>
        </div>
    <% } %>

    <div class="row col-md-12 col-md-offset-2 cstyle mx-auto">

        <h1 class="mx-auto"> 🌳 Your Schedule 🌳 </h1>

        <table class="table table-striped custab">
            <thead>
            <tr>
                <th>Name</th>
                <th>Post Code</th>
                <th>Date</th>
                <th>Time</th>

            </tr>

            <% if (parks.length > 0) {%>
            <%
            parks.forEach(park => {
            %>

                <tr>

                    <td><%= park.name %></td>
                    <td><%= park.postcode %></td>
                    <td><%= moment(park.date).format("Do MMMM YYYY") %></td>
                    <td><%= park.time %></td>

                    <td class="text-center">
                        <form action="/users/schedule/cancel/<%= park.id %>" method="POST">
                            <button class="btn btn-danger btn-xs" type="submit">Cancel</button>
                        </form>
                    </td>

                </tr>
            <%
            });
            %>
            <% } else {%>
                <tr>
                <td><h3>You are not saving any park for now.. Hurry and book your next hero act!</h3></td>
                </tr>
            <% }%>

            </thead>

        </table>


    </div>

</div>
<%- include('common/footer') -%>
</body>
</html>
